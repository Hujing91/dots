(defwidget screenshot []
    (eventbox
        :onclick { get_env("XDG_CURRENT_DESKTOP") == "Hyprland"
            ? "hyprctl dispatch nu ~/.config/screenshot/screenshot.nu output"
            : "swaymsg nu ~/.config/screenshot/screenshot.nu output" }
        :onmiddleclick { get_env("XDG_CURRENT_DESKTOP") == "Hyprland"
            ? "hyprctl dispatch nu ~/.config/screenshot/screenshot.nu window"
            : "swaymsg nu ~/.config/screenshot/screenshot.nu window" }
        :onrightclick { get_env("XDG_CURRENT_DESKTOP") == "Hyprland"
            ? "hyprctl dispatch nu ~/.config/screenshot/screenshot.nu area"
            : "swaymsg nu ~/.config/screenshot/screenshot.nu area" }
        (label
            :class "nerd-icon"
            :text "󰹑"
        )
    )
)