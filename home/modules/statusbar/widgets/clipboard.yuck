(defwidget clipboard []
    (eventbox
        :onclick { get_env("XDG_CURRENT_DESKTOP") == "Hyprland"
            ? "hyprctl dispatch nu ~/.config/clipman/open_history.nu"
            : "swaymsg nu ~/.config/clipman/open_history.nu" }
        (label
            :class "nerd-icon"
            :text "󱉫"
        )
    )
)